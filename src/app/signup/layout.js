export const metadata = {
  title: 'Create an Account | SurveyMonkey',
  description: 'Create your SurveyMonkey account to start creating and sending surveys.',
};

export default function SignupLayout({ children }) {
  return children;
} 