export const metadata = {
  title: 'Create Password | SurveyMonkey',
  description: 'Set up your password to complete your SurveyMonkey account creation.',
};

export default function PasswordLayout({ children }) {
  return children;
} 