export const metadata = {
  title: 'Sign in | SurveyMonkey',
  description: 'Sign in to your SurveyMonkey account to create surveys, collect responses, and analyze results.',
};

export default function LoginLayout({ children }) {
  return children;
} 